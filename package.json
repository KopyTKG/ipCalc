{
  "name": "ipcalc",
  "version": "1.0.0",
  "description": "IP calculator made with TypeScript",
  "main": "server.ts",
  "scripts": {
    "dev": "nodemon source/server.ts",
    "build": "rm -rf build/ && cd source/ && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "kopytkg",
  "license": "AGPL-3.0",
  "devDependencies": {
    "@types/node": "^20.8.6",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
